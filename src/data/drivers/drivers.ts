import { IDriver } from '../../common/interfaces/driver';
import {driver as driver1} from './driver_1';
import {driver as driver2} from './driver_2';

const DRIVERS: IDriver[] = [driver1, driver2];
export default DRIVERS;